export interface Columns {
    tools: string[];
    import_only: string[];
    both: string[];
    export_only: string[];
}

export interface UncheckedSupport {
    planned: string[];
    available: string[];
}
