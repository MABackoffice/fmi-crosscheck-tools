import { Colors } from "@blueprintjs/core";
import { StateController } from "../state";

export function supportColor(state: StateController, tool: string, exp: boolean): string {
    return Colors.GRAY5;
}
